# Struct With Union Average

This project uses a struct that contains a union to collect mixed type input (characters and floats). It then calculates the average of all float values entered.

## What I Learned
- How to embed a union inside a struct
- How to handle mixed type input using struct and union together
- How to calculate the average of selectively filtered values
- How to use type flags to distinguish between input types
